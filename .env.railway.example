# Railway Production Environment Variables Template
# Copy these to Railway Dashboard - DO NOT commit actual values!

# Database (Railway will auto-populate DATABASE_URL)
# DATABASE_URL=${{Postgres.DATABASE_URL}}

# Application
NODE_ENV=production
PORT=3000

# CORS - Update with your actual frontend domain
ALLOWED_ORIGINS=https://backstage-cine-frontend.railway.app,https://backstagecine.com

# JWT - GENERATE NEW SECRET!
# Run: node -e "console.log(require('crypto').randomBytes(64).toString('hex'))"
JWT_SECRET=REPLACE_WITH_GENERATED_SECRET
JWT_EXPIRES_IN=8h

# Rate Limiting
RATE_LIMIT_WINDOW_MS=900000
RATE_LIMIT_MAX_REQUESTS=100

# Logging
LOG_LEVEL=info
LOG_FORMAT=json
